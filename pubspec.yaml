name: flutter_local
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  
  cupertino_icons: ^1.0.8
  
  # YOLO detection for iOS/Android (CORRECTED VERSION)
  ultralytics_yolo: ^0.2.0
  
  # Camera access
  camera: ^0.10.5+9
  
  # Permissions handling
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/yolov8n.tflite
    - assets/models/metadata.yaml
    - android/app/src/main/assets/yolov8n_quant.tflite